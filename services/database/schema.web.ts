/**
 * Web stub — schema migrations are a no-op on web.
 * See index.web.ts for the full explanation.
 */
export const createTables = (): void => {
  // SQLite not available on web — nothing to migrate
};
